
import 'package:flame/components.dart';

final screenSize = Vector2(1920, 1080); 

// house helpers
final housePosition = Vector2(screenSize.x/4, 0); 
final houseSize = Vector2.all(400);

//plants helpers
final plantSize = Vector2.all(200);
final plantPosition = Vector2(screenSize.x/2, 80);

// garden helpers

final gardenSize = Vector2(600, 200);
final gardenPosition = Vector2(screenSize.x/2, 80);